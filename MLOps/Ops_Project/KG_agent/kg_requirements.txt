# Minimal dependencies for prod_pipeline.py running as a Cloud Run Job.
# Excludes: torch, transformers (HF backend not used), mlflow, pandas, scipy,
#           matplotlib (analysis only), pytest (tests only).
google-adk>=1.0.0
google-cloud-storage>=2.14.0
google-cloud-aiplatform>=1.71.0
neo4j>=5.0.0
python-dotenv>=1.0.0
langchain-core>=1.0.0
langchain-community>=0.4.0
langchain-experimental>=0.4.0
langchain-google-vertexai>=2.0.0
